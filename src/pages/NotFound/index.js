import React from 'react'
import { Layout } from '../../components'

export default function NotFound () {
  return <Layout
    title="Page not found 😓"
    subtitle='La página que buscas no existe'></Layout>
}